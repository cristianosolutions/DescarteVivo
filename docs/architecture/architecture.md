                           ┌─────────────────────────────────┐
                           │             Usuários             │
                           │  Web / Desktop / Mobile / Admin  │
                           └──────────────────┬───────────────┘
                                              │
                                      HTTP / HTTPS
                                              │
                                              ▼
                         ┌──────────────────────────────────┐
                         │          🌐 Frontend (React)      │
                         │ - Interface UI/UX                 │
                         │ - Axios / React-Router / Context │
                         │ - Auth via LocalStorage (JWT)     │
                         └──────────────┬───────────────────┘
                                        │  REST API (JSON)
                                        ▼
                     ┌────────────────────────────────────────────┐
                     │        🚀 Backend API (Node.js / Express)  │
                     │--------------------------------------------│
                     │  Controllers                               │
                     │  Services                                   │
                     │  Middleware (Auth JWT)                      │
                     │  Validations / Responses                   │
                     │  CORS / Segurança                          │
                     └──────────────┬─────────────────────────────┘
                                    │
                               Sequelize / pg / Pool
                                    │
                                    ▼
                       ┌───────────────────────────────────┐
                       │    🗄 Banco PostgreSQL (Railway)   │
                       │-----------------------------------│
                       │  Tabelas:                         │
                       │   - users                         │
                       │   - collection_points             │
                       │   - waste_deliveries              │
                       │   - waste_delivery_items          │
                       │   - waste_types                   │
                       │  Relacionamento via FK            │
                       └────────────────┬──────────────────┘
                                        │
                                   CI/CD Deploy
                                        │
                                        ▼
            ┌──────────────────────────────────────────────────────────┐
            │                 🌍 Infraestrutura de Deploy               │
            │  Frontend → Vercel | Backend + DB → Railway Cloud        │
            │  Build Pipeline + Auto Deploy via GitHub                 │
            └──────────────────────────────────────────────────────────┘
